'use strict';

/**
 * product-collections controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::product-collections.product-collections');
