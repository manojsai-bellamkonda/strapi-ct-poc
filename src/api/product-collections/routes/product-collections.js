'use strict';

/**
 * product-collections router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-collections.product-collections');
